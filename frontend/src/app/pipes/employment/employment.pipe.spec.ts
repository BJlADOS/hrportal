import { EmploymentPipe } from './employment.pipe';

describe('EmploymentPipe', () => {
  it('create an instance', () => {
    const pipe = new EmploymentPipe();
    expect(pipe).toBeTruthy();
  });
});
