import { VacancyDeclinationPipe } from './vacancy-declination.pipe';

describe('VacancyDeclinationPipe', () => {
  it('create an instance', () => {
    const pipe = new VacancyDeclinationPipe();
    expect(pipe).toBeTruthy();
  });
});
